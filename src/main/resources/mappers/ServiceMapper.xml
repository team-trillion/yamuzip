<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.trillion.yamuzip.service.model.dao.ServiceMapper">


    <select id="findAllService" resultType="team.trillion.yamuzip.service.model.dto.ServiceDTO">
        SELECT
            service_code,
            dob_nickname,
            service_title,
            service_explain,
            service_content,
            service_price,
            service_created
        FROM
            tbl_service
        JOIN tbl_dobby ON tbl_service.dob_code = tbl_dobby.dob_code
        WHERE
            service_status = 'A'
        ORDER BY
            service_code
    </select>
</mapper>